import { NoteContainer, Label } from "./styles";

export const Note = () => {

    return (
        <NoteContainer>
            <Label>Note</Label>
        </NoteContainer>
    );
};

export default Note;